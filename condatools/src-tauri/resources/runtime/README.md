# Runtime Folder

Place runtime binaries here before building desktop installers:

- backend.exe (built from ../backend/main.py)
- micromamba.exe

These files are intentionally not tracked in git.
Use scripts:
- ../backend/build_backend.ps1
- ../scripts/prepare_runtime.ps1
